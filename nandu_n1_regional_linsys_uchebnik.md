# Системы линейных уравнений — Учебник
## Olimpíada Ñandú · Nivel 1 · региональный этап
### Теория и разобранные примеры (8 глав + 2 дополнения)

> **Уровень.** Материал построен на базе регионального этапа Nivel 1 (корпус 2012–2025, см. `nandu_n1_regional_roadmap.md`): всё, что нужно для задач №1 этого уровня, и ничего сверх.
>
> **Как пользоваться.** Это первый из двух файлов. Здесь — объяснения и разобранные примеры. Во втором файле (`nandu_n1_regional_linsys_reshebnik.md`) — задачи для тренировки: блок задач к каждой главе.
>
> Порядок работы: прочитал главу → разобрал пример → решил блок задач → следующая глава.

---

## Правила игры (ритуалы) — читать первым, соблюдать всегда

Эти три привычки важнее любого метода. Они превращают «иногда получается» в «получается всегда».

**Ритуал 1 — нумеруй уравнения.**
Каждому уравнению — номер в кружочке: ①, ②, ③. Тогда можно коротко записывать, что ты сделал.

**Ритуал 2 — записывай ход.**
Каждое новое уравнение подписывай, откуда оно взялось: `① − ②`, `② × 3`, `① + ② + ③`. Это как запись партии в шахматах: всегда можно вернуться и найти ошибку.

**Ритуал 3 — проверяй подстановкой.**
Нашёл ответ — подставь его **во все исходные уравнения** (не в свои промежуточные!). Сошлось везде — ответ верный. Не сошлось хотя бы в одном — ищи ошибку. Проверка — это часть решения, а не «если останется время».

**И одна мысль над всеми ритуалами.** На самом деле мы всегда преобразуем **систему целиком**: каждый ход превращает её в новую систему с теми же решениями. Но переписывать всю систему на каждом шаге не нужно — её текущее состояние хранят номера: ① и ② никуда не исчезли с бумаги, а новое уравнение ③ подписано, откуда взялось. (В главе 8 увидишь запись, где состояние всей системы действительно видно на каждом шаге — и это дёшево, потому что там одни числа.)

> **Откуда берутся ошибки.** Почти никогда — из-за непонимания метода. Почти всегда — знак при вычитании (`7 − (−3)` это `10`, а не `4`) или потерянное слагаемое при переписывании. Ритуалы 1–2 ловят именно это.

---

## Глава 1. Канонизация: причеши систему

**Идея.** Прежде чем решать, приведи каждое уравнение к стандартному виду: **все неизвестные слева (в одном и том же порядке), число справа.** Уравнения в задачах приходят «лохматыми»: `b = a + 5`, `4a − (b + 6) = a + b`. Вычитать и складывать лохматые уравнения — верный путь запутаться в знаках.

**В этой главе мы ещё НЕ решаем системы.** Только причёсываем и останавливаемся. Способы решения начнутся с главы 2. Канонизация — первый ход почти любого решения, поэтому доводим её до автоматизма первой. (Почему «почти»: у одного хода — подстановки — будет своё исключение, увидишь его в главе 4.)

**Что разрешено делать с одним уравнением** (это весы в равновесии — равновесие сохраняют три действия):

1. **Перенести слагаемое** через `=` — со сменой знака.
2. **Раскрыть скобки** и привести подобные слагаемые.
3. **Умножить или разделить обе части** на одно и то же число (удобно, чтобы убрать дроби; по-настоящему заиграет в главе 3).

**Пример А (перенос).**

| Что делаем | Что получается |
|---|---|
| дано | `b = a + 5` |
| переношу `a` влево (знак меняется) | `−a + b = 5` |
| хочу первую букву с плюсом — умножаю всё на −1 | `a − b = −5` ✅ |

Обе последние записи — одно и то же уравнение, верны обе.

**Пример Б (лохматое).**

| Что делаем | Что получается |
|---|---|
| дано | `4a − (b + 6) = a + b` |
| раскрываю скобки | `4a − b − 6 = a + b` |
| переношу `a` и `b` влево, `−6` вправо | `4a − a − b − b = 6` |
| привожу подобные | `3a − 2b = 6` ✅ |

**Пример В (дроби).**

| Что делаем | Что получается |
|---|---|
| дано | `a/3 + b = 7` |
| умножаю обе части на 3 | `a + 3b = 21` ✅ |

После канонизации система «готова к бою» — и мы останавливаемся. Как решать — со следующей главы.

**Типичная ошибка №1:** перенести слагаемое и забыть сменить знак. Слагаемое «переезжает» через `=` только вместе со сменой знака.

**Типичная ошибка №2:** умножить левую часть и забыть правую. Умножается **всё уравнение целиком**.

---

## Глава 2. Сложение и вычитание уравнений

**Идея.** Уравнение — это весы в равновесии. Если сложить две пары уравновешенных весов — равновесие сохранится. Поэтому уравнения можно **складывать** и **вычитать** целиком: левое с левым, правое с правым.

Зачем? Чтобы одна неизвестная **исчезла**.

- Коэффициенты при неизвестной **одинаковые** → **вычитай**.
- Коэффициенты **противоположные** (`+y` и `−y`) → **складывай**.

**Пример А (сложение).**

Дано:
```
① x + y = 12
② x − y = 4
```

| Что делаем | Что получается |
|---|---|
| `① + ②` (у `y` противоположные знаки) | `2x = 16` → `x = 8` |
| подставляю `x = 8` в ① | `y = 12 − 8 = 4` |

**Ответ:** `x = 8, y = 4`
**Проверка:** `8 + 4 = 12` ✓, `8 − 4 = 4` ✓

**Пример Б (вычитание).**

Дано:
```
① 3x + 2y = 26
② x + 2y = 14
```

| Что делаем | Что получается |
|---|---|
| `① − ②` (у `2y` одинаковые коэффициенты) | `2x = 12` → `x = 6` |
| подставляю `x = 6` в ② | `2y = 14 − 6 = 8` → `y = 4` |

**Ответ:** `x = 6, y = 4`
**Проверка:** `18 + 8 = 26` ✓, `6 + 8 = 14` ✓

**Типичная ошибка:** при вычитании отнять только левые части, а правые сложить. Вычитаем ОБЕ стороны: `(3x + 2y) − (x + 2y) = 26 − 14`.

---

## Глава 3. Умножение уравнения на число

**Идея.** А если коэффициенты не совпадают нигде? Их можно **подогнать**: уравнение разрешается умножать целиком (каждое слагаемое!) на любое число — это то самое действие 3 из главы 1, но теперь оно из «причёски» превращается в боевой ход. Умножили так, чтобы коэффициенты при одной неизвестной совпали, — дальше глава 2.

**Пример А (умножаем одно уравнение).**

Дано:
```
① 2x + 3y = 21
② x + 2y = 13
```

| Что делаем | Что получается |
|---|---|
| хочу совпадения при `x`: `② × 2` | `③ 2x + 4y = 26` |
| `③ − ①` | `y = 5` |
| подставляю `y = 5` в ② | `x = 13 − 10 = 3` |

**Ответ:** `x = 3, y = 5`
**Проверка:** `6 + 15 = 21` ✓, `3 + 10 = 13` ✓

**Пример Б (умножаем оба — как НОК).**

Дано:
```
① 3a + 4b = 25
② 2a + 3b = 18
```

При `a` коэффициенты 3 и 2, общее кратное — 6.

| Что делаем | Что получается |
|---|---|
| `① × 2` | `③ 6a + 8b = 50` |
| `② × 3` | `④ 6a + 9b = 54` |
| `④ − ③` | `b = 4` |
| подставляю `b = 4` в ② | `2a = 18 − 12 = 6` → `a = 3` |

**Ответ:** `a = 3, b = 4`
**Проверка:** `9 + 16 = 25` ✓, `6 + 12 = 18` ✓

**Типичная ошибка:** умножить левую часть и забыть правую. Умножается **всё уравнение**, включая число справа.

---

## Глава 4. Подстановка

**Идея.** Если одна неизвестная уже выражена (`y = 2x + 1`) или легко выражается (коэффициент 1) — **подставь** это выражение во второе уравнение вместо неё. Останется одно уравнение с одной неизвестной.

**Подстановка — единственный ход, перед которым канонизация не нужна.** Вот то самое исключение, обещанное в главе 1. Для сложения и вычитания уравнение `y = 2x + 1` — «лохматое»: его пришлось бы причёсывать до `−2x + y = 1`. А для подстановки это, наоборот, идеальная форма: буква уже выражена — бери и подставляй. Причесав его, ты сделал бы шаг назад: чтобы подставить, пришлось бы выражать `y` заново. У системы, получается, две «боевые стойки»: **каноническая** — для сложения, вычитания и умножения, и **выраженная** (`буква = …`) — для подстановки. Причёсывай к той форме, которой собираешься бить.

**Как решить, причёсывать или нет.** Осмотри систему ДО канонизации:

- видишь уравнение вида «буква = выражение» (или букву с коэффициентом 1, которая выражается одним переносом) — это заявка на подстановку: такое уравнение не трогай, подставляй как есть (остальные уравнения системы, если они лохматые, причёсывай как обычно);
- ничего такого нет — канонизируй всё и работай сложением, вычитанием, умножением (главы 2–3).

Сомневаешься — канонизируй: канонизация никогда не ломает систему, худшее, что она делает, — добавляет пару лишних строк.

**Пример А (уже выражена).**

Дано:
```
① y = 2x + 1
② 3x + y = 16
```

| Что делаем | Что получается |
|---|---|
| подставляю ① в ② | `3x + (2x + 1) = 16` |
| привожу подобные | `5x = 15` → `x = 3` |
| подставляю `x = 3` в ① | `y = 2·3 + 1 = 7` |

**Ответ:** `x = 3, y = 7`
**Проверка:** `7 = 6 + 1` ✓, `9 + 7 = 16` ✓

**Пример Б (коэффициент 1 — выражаем сами).**

Дано:
```
① a + 3b = 14
② 2a + b = 13
```

| Что делаем | Что получается |
|---|---|
| из ① выражаю `a` | `a = 14 − 3b` |
| подставляю в ② | `2(14 − 3b) + b = 13` |
| раскрываю скобки | `28 − 6b + b = 13` |
| привожу подобные и решаю | `−5b = −15` → `b = 3` |
| подставляю `b = 3` в ① | `a = 14 − 9 = 5` |

**Ответ:** `a = 5, b = 3`
**Проверка:** `5 + 9 = 14` ✓, `10 + 3 = 13` ✓

### Подстановка дважды (и трижды)

Подстановка — ход многоразовый: подставил → система стала проще → смотри, нельзя ли подставить ещё раз. Так распутываются даже системы с тремя неизвестными, если связи «выражены» друг через друга.

**Пример В.**

Дано:
```
① c = a + b
② b = 2a
③ a + b + c = 30
```

| Что делаем | Что получается |
|---|---|
| в ③ вместо `a + b` подставляю `c` (это ①, прочитанное справа налево) | `c + c = 30` → `c = 15` |
| значит, ① теперь говорит | `a + b = 15` |
| подставляю в это ② (`b = 2a`) | `a + 2a = 15` → `a = 5` |
| из ② | `b = 2·5 = 10` |

**Ответ:** `a = 5, b = 10, c = 15`
**Проверка:** `15 = 5 + 10` ✓, `10 = 2·5` ✓, `5 + 10 + 15 = 30` ✓

Заметь: никакого нового метода — та же подстановка, просто два раза подряд. После каждой подстановки система уменьшается на одну букву.

**Забегая вперёд: подстановка и системы 3×3.** Пример В — уже система из трёх уравнений, и подстановка справилась. Так будет всегда, когда система «рыхлая»: в уравнениях по 1–2 буквы, что-то уже выражено. Такие системы мы будем распутывать в главе 6 — и главным инструментом там будет именно подстановка. А вот в «плотной» системе (во всех трёх уравнениях все три буквы) подстановка формально работает, но тонет в скобках: выражение с двумя буквами пришлось бы тащить сразу в два уравнения. Для плотных систем в главе 7 появится ход получше — исключение.

**Типичная ошибка:** подставить выражение без скобок. `2 × (14 − 3b)` — скобки обязательны, умножается всё выражение.

---

## Глава 5. Выбор хода (мета-навык)

**Идея.** Мастерство — это не «умею все три хода», а «за 5 секунд вижу, какой ход дешевле». Перед решением — быстрый осмотр системы:

| Что видишь | Ход |
|---|---|
| Противоположные коэффициенты (`+3y` и `−3y`) | **сложение** |
| Одинаковые коэффициенты (`5m` и `5m`) | **вычитание** |
| Неизвестная уже выражена (`y = …`) или коэффициент 1 | **подстановка** |
| Ничего не совпадает | **умножение** (одного или обоих), потом сложение/вычитание |

Осмотр делай **до** канонизации: вид `y = …` — это не «лохматость», а готовая подстановка (глава 4). Причёсывай только те уравнения, которые собираешься складывать или вычитать.

**Мини-тренировка «только назови ход» (не решая):**

- `2x + 5y = 19; 2x + 3y = 13` → вычитание
- `4a − 3b = 6; a + 3b = 24` → сложение
- `x = 3y − 2; 2x + y = 10` → подстановка
- `3m + 4n = 25; 5m + 6n = 39` → умножать оба

В решебнике есть целый блок таких: сначала называешь ход для всех задач подряд, и только потом решаешь.

---

## Глава 6. Три неизвестных — лёгкий вход: цепочки и «сложи всё»

**Идея.** Не всякая система 3×3 страшная. Часто она «рыхлая»: в каждом уравнении лишь 1–2 неизвестных. Такие системы решаются «распутыванием», и на них живут три быстрых приёма. У каждого приёма есть **опознавательный знак** — учись не только выполнять приём, но и узнавать его за несколько секунд осмотра.

### Приём «цепочка»

**Опознавательный знак:** есть уравнение, из которого одна буква находится сразу.

Дано:
```
① a = 3
② a + b = 10
③ b + c = 15
```

| Что делаем | Что получается |
|---|---|
| ① уже готово | `a = 3` |
| подставляю `a = 3` в ② | `b = 10 − 3 = 7` |
| подставляю `b = 7` в ③ | `c = 15 − 7 = 8` |

**Ответ:** `a = 3, b = 7, c = 8`

**Почему работает:** каждое следующее уравнение добавляет ровно одну новую букву. Это обычная подстановка из главы 4, просто несколько раз подряд. Если «стартового» уравнения (где буква находится сразу) нет — цепочка не заводится: смотри следующие приёмы.

### Приём «сложи всё»

**Опознавательный знак:** неизвестные ходят по кругу — каждая буква входит в систему «одинаково» (например, встречается ровно в двух уравнениях из трёх, всегда с коэффициентом 1).

Дано:
```
① a + b = 8
② b + c = 11
③ a + c = 13
```

| Что делаем | Что получается |
|---|---|
| `① + ② + ③` | `2a + 2b + 2c = 32` |
| делю на 2 | `④ a + b + c = 16` |
| `④ − ①` (пропадает `a + b`) | `c = 16 − 8 = 8` |
| `④ − ③` | `b = 16 − 13 = 3` |
| `④ − ②` | `a = 16 − 11 = 5` |

**Ответ:** `a = 5, b = 3, c = 8`
**Проверка:** `5 + 3 = 8` ✓, `3 + 8 = 11` ✓, `5 + 8 = 13` ✓

**Почему это помогает.** Каждое уравнение по отдельности «не видит» одну из букв. Но раз каждая буква встречается одинаково часто, при сложении всех уравнений перекос выравнивается: получается ровное кратное суммы — `2(a + b + c)`. А сумма `a + b + c` — волшебное четвёртое уравнение: в нём есть ВСЕ буквы, поэтому вычитание из него любого исходного уравнения мгновенно выдаёт ту букву, которой в этом уравнении не хватало.

**Как узнать за 10 секунд, сработает ли.** Просуммируй коэффициенты каждой буквы по всем уравнениям — «по столбцам». В примере: `a` входит с суммой `1 + 1 = 2`, `b` — `2`, `c` — `2`. Суммы равны → сложение даст ровное `2(a + b + c)` → приём сработает.

**Когда НЕ помогает:** если суммы по столбцам разные. Например:

```
① a + b = 8
② b + c = 11
③ a + 2c = 12
```

Суммы: у `a` — 2, у `b` — 2, у `c` — 3. Сложение всех уравнений даст `2a + 2b + 3c = 31` — это не кратное `a + b + c`, «волшебного» уравнения не получилось, буквы только перемешались. Здесь дешевле другой ход (скажем, `① − ②`: пропадёт `b`, останется система из двух уравнений с `a` и `c`).

### Приём «вычти сценарий»

**Опознавательный знак:** два уравнения — почти близнецы, различаются одним-двумя слагаемыми. Вычти их целиком — разница и выдаст это отличие.

Дано:
```
① x + y = 100
② x + y/2 = 70
```

| Что делаем | Что получается |
|---|---|
| `① − ②` (слева уцелеет `y − y/2`) | `y/2 = 30` → `y = 60` |
| подставляю `y = 60` в ① | `x = 100 − 60 = 40` |

**Ответ:** `x = 40, y = 60`

**Почему это помогает.** Оба уравнения описывают почти одну и ту же ситуацию — два «сценария», отличающихся одним пунктом. При вычитании всё одинаковое сокращается само собой, и остаётся ровно это отличие — маленькое уравнение с одной буквой. Заметь: «жертву» ты не выбирал, как будешь выбирать в главе 7, — сама форма системы подсказала пару, в которой при вычитании гибнет почти всё сразу.

**Как распознать.** Положи два уравнения рядом и сравни послагаемо. Отличие в одном месте (`y` против `y/2`) — вычитай: разность и будет этим отличием.

**Когда НЕ помогает:** если уравнения различаются почти всеми слагаемыми. Тогда разность выйдет такой же лохматой, как исходные уравнения, — выигрыша нет. Это не «сценарии», а обычная плотная система: решай её ходами глав 2–4, а если неизвестных три — методом главы 7.

(Пример выше — 2×2, чтобы механика была видна в чистом виде; сам приём не зависит от числа неизвестных, и в решебнике он встретится на трёх буквах. Запомни его: он главный ключ к задачам «если бы… то вышло бы…».)

### Чем это отличается от главы 7

По сути — ничем: те же законные ходы, подстановка, сложение, вычитание. Разница в том, **кто выбирает ход**. Здесь ход подсказывает сама форма системы, и один ход убивает почти всё. В главе 7 системы плотные, подсказок нет — там ту же работу придётся делать планомерно, исключая по одной букве. Поэтому у любой системы 3×3 такой **порядок осмотра**:

1. Есть уравнение, где буква находится сразу? → **цепочка**.
2. Суммы коэффициентов по столбцам равны? → **сложи всё**.
3. Два уравнения — почти близнецы? → **вычти сценарий**.
4. Ничего из этого? → честное исключение — **глава 7**.

**Мораль главы:** перед тем как «честно исключать», потрать 10 секунд на этот осмотр — может, система распутывается за три строчки.

---

## Глава 7. Полное 3×3: метод исключения

**Идея.** Если система «плотная» (во всех уравнениях все три неизвестных), план такой:

1. **Выбери жертву** — неизвестную, которую проще всего убрать (у которой коэффициенты совпадают или легко подгоняются).
2. **Убей её дважды:** из одной пары уравнений и из другой пары (складывая/вычитая, при необходимости умножая). Получишь **два** уравнения с **двумя** неизвестными.
3. **Реши 2×2** — это ты уже умеешь (главы 2–4).
4. **Обратный ход:** подставь найденное в любое исходное уравнение и найди третью неизвестную.
5. **Проверь во всех трёх исходных.**

**Пример.**

Дано:
```
① a + b + c = 14
② a + 2b + c = 18
③ 2a + b + 3c = 29
```

Жертва — `a`: в ① и ② у неё коэффициент 1.

| Что делаем | Что получается |
|---|---|
| `② − ①` (убил `a` в первый раз) | `b = 4` — повезло, сразу ответ! |
| `③ − ① × 2` (убил `a` во второй раз) | `−b + c = 1` |
| подставляю `b = 4` | `c = 1 + 4 = 5` |
| обратный ход: `b` и `c` в ① | `a = 14 − 4 − 5 = 5` |

**Ответ:** `a = 5, b = 4, c = 5`
**Проверка:** ① `5 + 4 + 5 = 14` ✓, ② `5 + 8 + 5 = 18` ✓, ③ `10 + 4 + 15 = 29` ✓

**Полезный трюк внутри 3×3: проверь суммы по столбцам (10 секунд).** Это тот же тест, что в главе 6, — и делать его стоит ДО выбора жертвы. Сложи коэффициенты каждой неизвестной по всем трём уравнениям. **Суммы равны** → сложи все три уравнения: получится ровное `k·(a + b + c)`, то есть сумма всех неизвестных, — а дальше вычитай её из исходных уравнений, и ответы посыплются сами. **Суммы разные** → трюк не поможет, работай по плану с жертвой, не трать время.

В примере выше суммы: у `a` — `1 + 1 + 2 = 4`, у `b` — `1 + 2 + 1 = 4`, у `c` — `1 + 1 + 3 = 5`. Не равны → складывать всё бессмысленно, поэтому мы и шли через жертву. А вот здесь:

```
① a + 2b + c = 16
② 2a + b + c = 14
③ a + b + 2c = 18
```

суммы `4, 4, 4` → `① + ② + ③` даёт `4a + 4b + 4c = 48`, то есть `④ a + b + c = 12`. Теперь `① − ④`: `b = 4`, `② − ④`: `a = 2`, `③ − ④`: `c = 6`. Вся система упала за четыре строчки — ни одной жертвы выбирать не пришлось.

**Типичная ошибка: проверка по своим следам.** По ходу решения ты создаёшь промежуточные уравнения (`④ = ② − ①`, `⑤ = ③ − ① × 2`…). Когда две неизвестные найдены, третью хочется достать из ближайшего уравнения — а ближайшее как раз промежуточное. Так делать нельзя. Если при выводе ④ ты ошибся в арифметике, то значения, найденные из ④, идеально «сойдутся» с самим ④ — ошибка подтвердит сама себя, и ты её не заметишь. Исходные уравнения ①②③ — единственный «оригинал», в котором ошибки быть не может: их написал не ты, а задача. Поэтому обратный ход (поиск последней неизвестной) — только в исходное уравнение, а финальная проверка (ритуал 3) — во все три исходных.

---

## Глава 8. Табличная запись и треугольная форма

**Идея.** Это **не новый метод**. Это те же ходы из глав 2–7, но записанные компактно: буквы не переписываем, храним только числа-коэффициенты в таблице. Меньше писанины — меньше ошибок. И помнишь мысль из «Правил игры» — что мы всегда преобразуем систему целиком? Здесь это видно буквально: каждый снимок таблицы — это вся система на текущем шаге.

Система из главы 7 в виде таблицы (столбцы: `a`, `b`, `c`, правая часть):

```
        a   b   c │
①       1   1   1 │ 14
②       1   2   1 │ 18
③       2   1   3 │ 29
```

Каждая строка — уравнение, каждый столбец — своя буква, вертикальная черта — знак `=`. Пустых мест не бывает: если какой-то буквы в уравнении нет, в её столбце пишем 0.

### Ходы со строками

Со строкой таблицы разрешено ровно то же, что с уравнением, плюс один «бесплатный» ход:

1. **Вычесть из строки другую строку, умноженную на число** (или прибавить): `③ − 2·①`. Действие идёт по всем четырём столбцам, включая число за чертой!
2. **Умножить или разделить строку на число** (не на ноль) — тоже все четыре числа.
3. **Поменять две строки местами.** Система — это набор уравнений, порядок записи не важен. (С уравнениями так было можно всегда — просто до таблиц это ни разу не требовалось.)

### Цель: треугольная форма

Приводим таблицу к «лесенке» — под диагональю одни нули:

```
■   ■   ■ │ ■
0   ■   ■ │ ■
0   0   ■ │ ■
```

Зачем: нижняя строка такой таблицы — уравнение с одной буквой, то есть готовый ответ; строка над ней добавляет ровно одну новую букву, и так далее вверх. Это в точности «цепочка» из главы 6 — а цепочки ты решаешь легко.

### Правила приведения: столбец за столбцом, слева направо

**Шаг 1 — чистим первый столбец.** Рабочая строка — ①, её не меняем. Из каждой строки ниже вычитаем такое кратное строки ①, чтобы в первом столбце этой строки встал 0. Кратное подсказывает сама таблица: стоит у ② в первом столбце 1 (как у ①) — вычитаем ① один раз; стоит 2 — вычитаем `2·①`; стоит 3 — вычитаем `3·①`.

**Шаг 2 — чистим второй столбец ниже диагонали.** Рабочая строка теперь ②′ (уже обновлённая!); строки ① и ②′ больше не трогаем. Из строк ниже вычитаем кратное строки ②′ так, чтобы во втором столбце встал 0.

Почему на шаге 2 не портятся нули первого столбца: у рабочей строки ②′ в первом столбце уже стоит 0, а вычитание нуля ничего не меняет. В этом весь смысл порядка «слева направо»: каждый столбец чистим строкой, у которой все предыдущие столбцы уже нулевые.

**Шаг 3 и дальше** — то же самое для следующих столбцов. В 3×3 после шага 2 всё готово; в 4×4 будет ещё один шаг (см. дополнение Б).

**Два запасных хода для неудобных чисел:**

- В рабочей позиции стоит **0** (например, в первой строке вообще нет первой буквы) → **поменяй строки местами**: подними наверх строку, где число есть.
- Кратное **не подбирается** (у рабочей строки 2, ниже стоит 3) → иди через НОК, как в главе 3: `2·(нижняя) − 3·(рабочая)`. Умножать строки — законный ход 2.

### Пример 1 — кратные подбираются сразу

Таблица выше. **Шаг 1**, чистим первый столбец строкой ①: у ② в нём стоит 1 → ход `② − ①`; у ③ стоит 2 → ход `③ − 2·①`:

```
①       1   1   1 │ 14
②'      0   1   0 │  4
③'      0  −1   1 │  1
```

**Шаг 2**, чистим второй столбец строкой ②′: у ③′ во втором столбце стоит −1 → прибавляем ②′ один раз: `③′ + ②′`:

```
①       1   1   1 │ 14
②'      0   1   0 │  4
③''     0   0   1 │  5
```

Получилась **треугольная форма**: под диагональю — нули. Читаем снизу вверх (**обратный ход**):

- ③'': `c = 5`
- ②': `b = 4`
- ①: `a + 4 + 5 = 14` → `a = 5`

### Пример 2 — понадобятся НОК и умножение строки

```
        x   y   z │
①       1   1   1 │  6
②       2   4   3 │ 19
③       3   6   4 │ 27
```

**Шаг 1**, чистим первый столбец строкой ①: `② − 2·①` и `③ − 3·①`:

```
①       1   1   1 │  6
②'      0   2   1 │  7
③'      0   3   1 │  9
```

**Шаг 2**, чистим второй столбец строкой ②′. У ②′ во втором столбце 2, у ③′ — 3: целое кратное не подбирается. Запасной ход — НОК(2, 3) = 6: `2·③′ − 3·②′`:

```
①       1   1   1 │  6
②'      0   2   1 │  7
③''     0   0  −1 │ −3
```

Треугольная форма готова. Обратный ход снизу вверх:

- ③'': `−z = −3` → `z = 3`
- ②': `2y + 3 = 7` → `y = 2`
- ①: `x + 2 + 3 = 6` → `x = 1`

**Проверка по исходным:** `1 + 2 + 3 = 6` ✓, `2 + 8 + 9 = 19` ✓, `3 + 12 + 12 = 27` ✓.

**Заметь:** обратный ход по треугольной таблице — это в точности «цепочка» из главы 6. Круг замкнулся: весь метод исключения — это способ превратить любую плотную систему в цепочку, а цепочки ты решаешь легко.

**Зачем это, если и буквами получалось?** На 2×2 — незачем. На 3×3 таблица экономит половину писанины и дисциплинирует: видно каждый ход, ошибку найти легко. И это тот же способ, которым системы решают «взрослые» математики и компьютеры — только они называют таблицу **матрицей**.

---

## Дополнение А. Странные случаи: `0 = 0` и `0 = 5`

Обычно система имеет ровно одно решение. Но при вычитании уравнений может получиться странное.

**Случай `0 = 0`** (всё исчезло, и это правда):

```
① x + y = 5
② 2x + 2y = 10
```

- `② − ① × 2`: `0 = 0`

Второе уравнение — то же самое, что первое, только умноженное на 2. Новой информации нет: подходит и `x = 1, y = 4`, и `x = 2, y = 3`… **Решений бесконечно много** — система «недоопределена».

**Случай `0 = 5`** (всё исчезло, и это неправда):

```
① x + y = 5
② 2x + 2y = 15
```

- `② − ① × 2`: `0 = 5`

Но ноль не равен пяти! Уравнения **противоречат** друг другу: **решений нет**.

**Практический вывод — самый важный:** если в честной задаче у тебя вылезло `0 = 3` — это почти наверняка не «решений нет», а **сигнал: ошибка в арифметике, вернись по записи ходов и найди её**. (Вот зачем ритуал 2.)

---

## Дополнение Б. А если неизвестных четыре?

На олимпиаде Ñandú Nivel 1 за 14 лет ни разу не было больше трёх неизвестных. Но секрет в том, что **метод вообще не зависит от размера**: исключил одну неизвестную — из 4×4 получилось 3×3, а его ты умеешь. Из 5×5 получилось бы 4×4, и так далее. Лестница одна и та же, просто ступенек больше.

Убедись, что ничего нового нет — система 4×4 в треугольной форме:

```
① a + b + c + d = 24
②     b + c + d = 17
③         c + d = 11
④             d = 4
```

Обратный ход снизу вверх:

- ④: `d = 4`
- ③: `c = 11 − 4 = 7`
- ②: `b = 17 − 7 − 4 = 6`
- ①: `a = 24 − 6 − 7 − 4 = 7`

Четыре неизвестных, а решение — обычная цепочка.

Так что если когда-нибудь встретишь 4 неизвестных — не пугайся: это не новая тема, а лишняя ступенька той же лестницы.

---

## Карта курса

| Глава | Навык | Блок в решебнике |
|---|---|---|
| 1 | Канонизация: привести к стандартному виду (без решения) | Блок 1 (9 задач) |
| 2 | Сложение и вычитание уравнений | Блок 2 (10 задач) |
| 3 | Умножение уравнения на число | Блок 3 (9 задач) |
| 4 | Подстановка (в том числе дважды подряд) | Блок 4 (9 задач) |
| 5 | Выбор хода | Блок 5 (10 задач) |
| 6 | Цепочки, «сложи всё», «вычти сценарий» + осмотр системы | Блок 6 (11 задач) |
| 7 | Полное 3×3 исключением | Блок 7 (8 задач) |
| 8 | Таблица и треугольная форма | Блок 8 (9 задач) |
| Доп. А, Б | Странные случаи и 4×4 | Бонус-блок (8 задач) |

Сквозные ритуалы (нумерация, запись ходов, проверка) — на каждой задаче каждого блока, без исключений.
