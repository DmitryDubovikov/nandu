# Делимость, НОД и НОК — Решебник

## Математика, 5 класс · повторение

> **Как тренироваться.** Блок соответствует главе учебника (`math5_delimost_uchebnik.md`). Прочитал главу → решаешь её блок целиком, подряд. В каждом блоке 3 задачи: сначала на технику, последняя — текстовая или с ловушкой.
>
> **На каждой задаче:** признаки на `2`, `5`, `10` — только последняя цифра; на `3` и `9` — только сумма цифр; не помнишь правило — разложи на простые множители.
>
> **Ответы — в конце файла.** Смотреть после того, как решил весь блок.

---

## Блок 1 — Делители и кратные (глава 1)

**1.1** Выпиши все делители числа `36`. Сколько их получилось?

**1.2** Выпиши все кратные числа `7`, которые меньше `50`.

**1.3** В коробке `48` конфет. Их надо разложить поровну в пакеты так, чтобы пакетов было больше одного и в каждом пакете было больше одной конфеты. Сколькими способами это можно сделать?

---

## Блок 2 — Признаки делимости на 2, 5 и 10 (глава 2)

**2.1** Из чисел `130`, `245`, `918`, `1005`, `3724` выпиши отдельно те, что делятся на `2`; на `5`; на `10`.

**2.2** Вместо звёздочки поставь цифру так, чтобы число `47*` делилось на `5`. Найди все варианты.

**2.3** Какие цифры можно поставить вместо звёздочки в числе `3*6`, чтобы оно делилось на `2`?

---

## Блок 3 — Признаки делимости на 3 и 9 (глава 3)

**3.1** Какие из чисел `261`, `4032`, `5140`, `741` делятся на `3`? А какие на `9`?

**3.2** Вместо звёздочки поставь цифру так, чтобы число `51*` делилось на `9`. Найди все варианты.

**3.3** Число `2*4` делится на `3`. Найди все цифры, которые могут стоять вместо звёздочки.

---

## Блок 4 — Простые числа и разложение на множители (глава 4)

**4.1** Выпиши все простые числа от `20` до `40`.

**4.2** Разложи на простые множители числа `84` и `360`.

**4.3** Число разложили на простые множители и получилось `2 × 2 × 3 × 5`. Что это за число? Делится ли оно на `15`? Ответь, не деля.

---

## Блок 5 — НОД и взаимно простые числа (глава 5)

**5.1** Найди НОД(`48`, `60`).

**5.2** Взаимно просты ли числа `25` и `42`? А числа `24` и `39`?

**5.3** Из `36` красных и `48` синих шаров собирают одинаковые букеты, использовав все шары без остатка. Какое наибольшее число букетов получится? Сколько шаров каждого цвета будет в одном букете?

---

## Блок 6 — НОК (глава 6)

**6.1** Найди НОК(`12`, `18`).

**6.2** Найди НОК(`8`, `15`).

**6.3** Один автобус приходит на остановку каждые `12` минут, другой — каждые `20` минут. Сейчас они пришли вместе. Через сколько минут они снова придут вместе?

---

# Ответы

## Блок 1

**1.1** `1, 2, 3, 4, 6, 9, 12, 18, 36` — всего `9` делителей.

**1.2** `7, 14, 21, 28, 35, 42, 49`.

**1.3** `8` способов. Делителей у `48` десять: `1, 2, 3, 4, 6, 8, 12, 16, 24, 48`. Каждый делитель — это возможное число пакетов. Отбрасываем два: `1` пакет (пакетов должно быть больше одного) и `48` пакетов (тогда в каждом по одной конфете). Остаётся `10 − 2 = 8`.

## Блок 2

**2.1** На `2`: `130`, `918`, `3724`. На `5`: `130`, `245`, `1005`. На `10`: только `130`.

**2.2** Две цифры: `0` и `5`, то есть `470` и `475`.

**2.3** **Любую** из десяти цифр `0–9`. Ловушка: делимость на `2` определяет последняя цифра, а она здесь уже стоит и равна `6` — чётная. Что бы ни стояло в середине, число делится на `2`.

## Блок 3

**3.1** Суммы цифр: `261 → 9`, `4032 → 9`, `5140 → 10`, `741 → 12`.
На `3` делятся: `261`, `4032`, `741`. На `9` делятся: `261`, `4032`.
Число `741` — как раз тот случай, когда на `3` делится, а на `9` нет.

**3.2** Одна цифра: `3`, то есть `513`. Сумма цифр `5 + 1 + * = 6 + *` должна делиться на `9`; из цифр `0–9` подходит только `* = 3` (сумма `9`). Следующая подходящая сумма `18` потребовала бы `* = 12` — такой цифры нет.

**3.3** Четыре цифры: `0, 3, 6, 9`, то есть числа `204`, `234`, `264`, `294`. Сумма `2 + * + 4 = 6 + *` делится на `3`, а `6` уже делится — значит `*` тоже должна делиться на `3`.

## Блок 4

**4.1** `23, 29, 31, 37`.

**4.2** `84 = 2 × 2 × 3 × 7`; `360 = 2 × 2 × 2 × 3 × 3 × 5`.

**4.3** Это число `60`. На `15` делится: `15 = 3 × 5`, а в разложении `60` есть и множитель `3`, и множитель `5` — значит их произведение тоже делитель.

## Блок 5

**5.1** НОД `= 12`. Разложения: `48 = 2 × 2 × 2 × 2 × 3`, `60 = 2 × 2 × 3 × 5`. Общие множители в наименьшей степени: две двойки и одна тройка → `2 × 2 × 3 = 12`.

**5.2** `25` и `42` — **взаимно простые**: `25 = 5 × 5`, `42 = 2 × 3 × 7`, общих множителей нет, НОД `= 1`.
`24` и `39` — **не взаимно простые**: `24 = 2 × 2 × 2 × 3`, `39 = 3 × 13`, общая тройка, НОД `= 3`.

**5.3** Букетов `12` — это НОД(`36`, `48`). В каждом букете `36 : 12 = 3` красных и `48 : 12 = 4` синих шара.

## Блок 6

**6.1** НОК `= 36`. `12 = 2 × 2 × 3`, `18 = 2 × 3 × 3`; берём все множители в наибольшей степени: две двойки и две тройки → `2 × 2 × 3 × 3 = 36`.

**6.2** НОК `= 120`. Числа `8 = 2 × 2 × 2` и `15 = 3 × 5` взаимно простые, поэтому НОК — просто их произведение: `8 × 15 = 120`.

**6.3** Через `60` минут — это НОК(`12`, `20`). `12 = 2 × 2 × 3`, `20 = 2 × 2 × 5` → `2 × 2 × 3 × 5 = 60`.
