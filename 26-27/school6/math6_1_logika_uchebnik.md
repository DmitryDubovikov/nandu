# Логика — Учебник

## Математика, 6 класс

> **Что здесь.** Десять коротких глав про то, как устроены математические утверждения: отрицание, кванторы «все» и «существует», логическое следование «если …, то …», обратное утверждение и равносильность. Это новая для тебя тема — начни с раздела «Что такое высказывание».
>
> **Как пользоваться.** Прочитал главу → разобрал пример → решил её блок в решебнике (`math6_1_logika_reshebnik.md`). В блоке 3 задачи.
>
> **Что нужно знать заранее:** делимость, признаки делимости, простые числа, взаимно простые числа; квадрат и прямоугольник; буква вместо неизвестного числа.

---

## Что такое высказывание — читать первым

Представь спор. Один говорит: «В феврале 30 дней», другой: «Нет, не 30». Спор осмысленный: можно пойти и проверить, кто прав. А если один скажет «Открой окно», спорить не о чем — это не то предложение, про которое бывает «правда» и «неправда».

**Высказывание** — это предложение, про которое можно сказать одно из двух: оно **истинно** или оно **ложно**. Третьего не бывает, и обоих сразу тоже не бывает.

| Предложение | Высказывание? | Почему |
|---|---|---|
| «Число `24` делится на `6`» | да, истинное | проверяется: `24 : 6 = 4` |
| «В феврале `30` дней» | да, ложное | проверяется: в феврале `28` или `29` |
| «Сколько будет `7 × 8`?» | нет | вопрос, а не утверждение |
| «Открой тетрадь» | нет | приказ, а не утверждение |
| «Математика интереснее рисования» | нет | нет способа проверить, это мнение |
| «Число `a` больше `7`» | нет | пока не сказано, чему равно `a` |

Про строку с «интереснее» скажем отдельно, потому что это целый разряд фраз. **Оценочное суждение — не высказывание.** Слова «красивый», «интересный», «лучший», «скучный» не задают никакой проверки: два человека ответят по-разному, и оба будут по-своему правы. Спорить о таких фразах можно, но выяснить, истинны они или ложны, нельзя — значит в математике они не высказывания. Проверить себя легко: спроси, можно ли договориться заранее, какая именно проверка решит спор. Для «`24` делится на `6`» такая проверка есть, для «математика интереснее рисования» — нет.

Обрати внимание на две последние строки таблицы — они разные.

Про «интереснее» нельзя ответить «истинно» или «ложно» вообще никак. А вот «`a` больше `7`» — это **предложение с переменной**: как только скажешь, чему равно `a`, оно тут же становится высказыванием. Подставь `a = 9` — истинное. Подставь `a = 2` — ложное. Само по себе, без значения `a`, оно ни истинно, ни ложно.

**Ложное — тоже высказывание.** Высказывание — это не «правильная фраза», а фраза, для которой вопрос «правда или нет?» имеет ответ.

**Зачем нужна эта тема.** Дальше в математике почти всё сказано двумя способами: «неверно, что …» и «если …, то …». Первое нужно, чтобы уметь **опровергать**: пока не умеешь аккуратно построить отрицание, не знаешь, что именно надо найти, чтобы утверждение рухнуло. Второе — чтобы уметь **пользоваться правилами**: любое правило вида «признак делимости», «свойство фигуры» — это следование, и половина ошибок берётся из того, что его читают задом наперёд.

### Мини-задача

Какие из фраз — высказывания? Для каждого высказывания скажи, истинно оно или ложно.

1. «Число `24` делится на `6`.»
2. «Сколько дней в неделе?»
3. «Число `a` больше `7`.»
4. «В неделе `8` дней.»
5. «Нарисуй треугольник.»

**Шаг 1 — план.** Нужно рассортировать пять фраз — для этого прогоню каждую через один вопрос: «можно ли прямо сейчас ответить, истинна она или ложна?» Кто отвечает — высказывание, кто нет — не высказывание.

> ✅ **План готов:** Ответ = список тех фраз, что прошли вопрос, плюс вердикт «истинно/ложно» для каждой из них.

**Шаг 2 — убрать то, что вообще не утверждение.** Нужно отсеять фразы, которые ничего не заявляют. Фраза 2 — вопрос, фраза 5 — приказ. Про них нельзя сказать ни «правда», ни «неправда».
→ **выбывают 2 и 5**

**Шаг 3 — убрать предложение с переменной.** Нужно отсеять то, что зависит от неизвестного числа. Фраза 3 содержит букву `a`, и ответ меняется вместе с ней: при `a = 9` истинна, при `a = 2` ложна. Значит сама по себе она не высказывание.
→ **выбывает 3**

**Шаг 4 — вердикты оставшимся.** Нужно проверить фразы 1 и 4. `24 : 6 = 4` ровно → фраза 1 истинна. В неделе `7` дней → фраза 4 ложна.
→ **1 — истинно, 4 — ложно**

**Шаг 5 — сборка.** Высказывания — это фразы **1 и 4**: первая истинная, вторая ложная. Фразы 2, 3, 5 высказываниями не являются.

---

## Правила игры (ритуалы) — читать первым, соблюдать всегда

**Ритуал 1 — сначала спроси: это вообще высказывание?** Если про фразу нельзя ответить «истинно/ложно», обсуждать её отрицание бессмысленно. «Число `15` делится на `4`» — высказывание (ложное). «На что делится `15`?» — не высказывание.

**Ритуал 2 — отрицание строй в два хода.** Сначала буквально: «Неверно, что …». Потом переведи на нормальный язык. «Неверно, что `15` делится на `4`» → «`15` не делится на `4`».

**Ритуал 3 — проверяй парой.** Высказывание и его отрицание не бывают истинными вместе и не бывают ложными вместе: ровно одно из двух истинно. Получилось, что оба ложны, — значит ты построил не отрицание, а что-то другое.

**Ритуал 4 — «все» опровергается одним, доказывается всеми.** Один **контрпример** — то есть найденный объект, на котором обещание «все» ломается, — убивает утверждение «все». А сколько бы примеров ни подтвердили его, они его не докажут. «Все двузначные числа, оканчивающиеся на `5`, делятся на `3`»: `15` делится, `45` делится — а `25` нет. Два подтверждения не спасли.

**Ритуал 5 — увидел «если …, то …» — назови A и B вслух.** «Если число делится на `10`, то оно чётное»: A (условие) — «число делится на `10`», B (заключение) — «число чётное». Половина ошибок в этой теме — от того, что A и B перепутаны местами.

---

## Глава 1. Понятие отрицания

**Отрицание** высказывания — это высказывание «неверно, что …». Отрицание высказывания «`7` — простое число» — это «неверно, что `7` — простое число», по-человечески «`7` — не простое число».

Главное свойство: **истинность переворачивается**. Исходное истинно → отрицание ложно. Исходное ложно → отрицание истинно. Никогда не бывает так, чтобы истинными были оба или ложными были оба.

Отсюда сразу: отрицание отрицания возвращает то же самое. Истинность переворачивается один раз, потом второй — и оказывается ровно там, откуда начали. Смотри: «`6` делится на `3`» истинно → «`6` не делится на `3`» ложно → «Неверно, что `6` не делится на `3`» снова истинно. Последняя фраза — это просто «`6` делится на `3`».

**Осторожно: отрицание — это не «противоположное».** Отрицание высказывания «`8` больше `5`» — это «`8` не больше `5`», то есть «`8` меньше `5` **или** равно `5`». А не «`8` меньше `5`»: между «больше» и «меньше» затерялось «равно», и его нельзя терять.

| Высказывание | Отрицание | Не отрицание! |
|---|---|---|
| число больше `5` | число не больше `5` (меньше или равно) | число меньше `5` |
| отрезок длиннее `3` см | отрезок не длиннее `3` см | отрезок короче `3` см |
| число чётное | число нечётное | — здесь совпало: других вариантов нет |

Последняя строка показывает, когда «противоположное» всё же годится: если случаев ровно два и третьего не дано.

### Разбор

Построй отрицание высказывания «Число `91` — простое» и определи, какое из двух истинно.

**Шаг 1 — план.** Нужно получить пару «высказывание — отрицание» и раздать вердикты. Отрицание строю по форме «неверно, что …». Вердикт проверяю **у одного** из них, а второй получаю переворотом.

> ✅ **План готов:** Ответ = отрицание, записанное по-человечески, + вердикт исходному + перевёрнутый вердикт отрицанию.

**Шаг 2 — построить отрицание.** Нужна фраза «неверно, что …»: «Неверно, что число `91` — простое». Перевожу: «Число `91` — не простое», то есть «`91` — составное».
→ **отрицание: «`91` — составное число»**

**Шаг 3 — проверить исходное.** Нужно узнать, простое ли `91`. Пробую делители: на `2`, `3`, `5` не делится; `91 : 7 = 13` ровно. Значит у `91` есть делитель `7`, кроме `1` и себя.
→ **исходное ложно**

**Шаг 4 — сборка.** Исходное ложно, значит отрицание истинно.
Ответ: отрицание — «`91` — составное число»; исходное высказывание ложно, отрицание истинно.

**Типичная ошибка:** отрицать «число больше `5`» словами «число меньше `5`». Потерялся случай «число равно `5`». Проверь парой: возьми число `5`. «`5` больше `5`» — ложно. «`5` меньше `5`» — тоже ложно. Оба ложны одновременно — значит это не пара «высказывание и его отрицание».

---

## Глава 2. Отрицание общих высказываний

**Общее высказывание** говорит про **все** объекты сразу. Слова-приметы: «все», «каждый», «любой», а также «ни один» (это «все … не …»).

Отрицание общего высказывания — это **утверждение о существовании**:

> Отрицание «все A обладают свойством B» = «**найдётся хотя бы один** A, который свойством B **не** обладает».

Такой найденный объект называется **контрпримером**.

Например, отрицание высказывания «Все простые числа нечётные» — это «Найдётся простое чётное число». И такое есть: `2`. Значит исходное ложно.

**Заметь главное.** Опровергать и доказывать «все» — работа разного размера. Чтобы **опровергнуть** «все», хватит **одного** контрпримера: одно исключение — и обещание «все» нарушено. А чтобы **доказать** «все», примеров не хватит никогда, сколько бы их ни набралось: нужен либо перебор всех объектов до последнего (если их конечное число), либо общая причина.

### Разбор

Построй отрицание высказывания «Все числа, оканчивающиеся на `3`, — простые» и определи, какое из двух истинно.

**Шаг 1 — план.** Нужно получить отрицание и вердикт. Отрицание общего высказывания — это существование контрпримера, значит вердикт сводится к поиску: нашёл контрпример → исходное ложно, отрицание истинно; убедился, что контрпримеров нет, → наоборот.

> ✅ **План готов:** Ответ = отрицание в форме «найдётся …» + вердикт по итогу поиска контрпримера.

**Шаг 2 — построить отрицание.** Нужна форма «найдётся A без свойства B». Здесь A — «число оканчивается на `3`», B — «число простое».
→ **отрицание: «Найдётся число, оканчивающееся на `3`, которое не является простым»**

**Шаг 3 — искать контрпример.** Нужно составное число с последней цифрой `3`. Иду по порядку: `3` — простое, `13` — простое, `23` — простое, `33 = 3 × 11` — составное.
→ **контрпример: `33`**

**Шаг 4 — сборка.** Контрпример найден, значит исходное высказывание ложно, а отрицание истинно.
Ответ: отрицание — «Найдётся число, оканчивающееся на `3`, которое не является простым»; исходное ложно (контрпример `33`), отрицание истинно.

Посмотри, как шёл шаг 3: три числа подряд подтвердили высказывание — и не доказали ровным счётом ничего. А четвёртое закрыло вопрос навсегда.

**Типичная ошибка 1:** записать отрицание высказывания «Все A обладают B» как «Все A **не** обладают B». Это другое высказывание, и оно может быть ложным одновременно с исходным. «Все простые числа нечётные» — ложно (`2`). «Все простые числа чётные» — тоже ложно (`3`). Оба ложны, значит отрицанием тут и не пахнет.

**Типичная ошибка 2:** проверить несколько примеров и объявить «все» доказанным. Проверка примеров может только опровергнуть — подтвердить она не умеет.

---

## Глава 3. Отрицание высказываний о существовании

**Высказывание о существовании** говорит, что нужный объект хотя бы один есть. Слова-приметы: «существует», «найдётся», «есть», «некоторые», «хотя бы один».

Со словом «некоторые» будь внимателен: в математике оно читается не так, как в разговоре. В разговоре «некоторые» намекает «не все». В математике «некоторые A обладают B» значит ровно «хотя бы один A обладает B» — а сколько их на самом деле, один, половина или вообще все, высказывание не уточняет. Поэтому «Некоторые натуральные числа больше нуля» — истинное высказывание, хотя больше нуля они все до единого.

Отрицание такого высказывания — **общее**:

> Отрицание «существует A со свойством B» = «**ни один** A не обладает свойством B» = «все A свойством B не обладают».

Отрицание переворачивает тип высказывания. Вот вся таблица темы. В третьем столбце сказано, какой работой подтверждается высказывание из **первого** столбца — не из второго.

| Высказывание | Его отрицание | Чем подтверждается высказывание из первого столбца |
|---|---|---|
| все A обладают B | найдётся A без B | проверкой всех A |
| найдётся A с B | ни один A не обладает B | одним примером |

**Заметь главное.** Работа тоже переворачивается: чтобы **доказать** существование, хватает **одного** примера; чтобы **опровергнуть** его, нужно разобраться со всеми объектами сразу. Сравни с главой про «все», где всё наоборот: там один пример ничего не доказывает, зато один контрпример всё опровергает.

Пример на опровержение: «Существует простое число, которое делится на `6`». Любое число, делящееся на `6`, имеет среди делителей `1`, `2`, `3` и само себя — это уже больше двух делителей, значит простым оно быть не может. Ни одного примера нет, высказывание ложно, а его отрицание «Ни одно простое число не делится на `6`» истинно.

### Разбор

Построй отрицание высказывания «Среди чисел от `1` до `20` есть число, у которого ровно три делителя» и определи, какое из двух истинно.

**Шаг 1 — план.** Нужно отрицание и вердикт. Отрицание высказывания о существовании — общее высказывание «ни одно …». Вердикт: исходное подтверждается одним примером, поэтому сначала попробую его найти.

> ✅ **План готов:** Ответ = отрицание в форме «ни одно …» + вердикт по итогу поиска примера.

**Шаг 2 — построить отрицание.** Форма «ни один A не обладает B»: A — «число от `1` до `20`», B — «ровно три делителя».
→ **отрицание: «Ни у одного числа от `1` до `20` не бывает ровно трёх делителей»**

**Шаг 3 — искать пример.** Нужно число с ровно тремя делителями. Иду по числам подряд. У `1` делитель ровно один — сама единица. У `2` делители `1` и `2` — их два. У `3` делители `1` и `3` — тоже два. У `4` делители `1`, `2` и `4` — ровно три, это то, что нужно.
→ **пример: `4`**

**Шаг 4 — сборка.** Пример найден, значит исходное истинно, а отрицание ложно.
Ответ: отрицание — «Ни у одного числа от `1` до `20` не бывает ровно трёх делителей»; исходное истинно (пример `4`, годится и `9` с делителями `1`, `3`, `9`), отрицание ложно.

**Типичная ошибка:** отрицать «некоторые ученики опоздали» словами «некоторые ученики не опоздали». Оба этих высказывания легко истинны одновременно — если часть класса опоздала, а часть нет. Отрицание здесь одно: «ни один ученик не опоздал».

---

## Глава 4. Переменная и кванторы

Предложение с переменной — например, «число `n` делится на `3`» — высказыванием не является, пока не сказано, что за `n`. Превратить его в высказывание можно двумя способами.

**Способ 1 — подставить значение.** «`12` делится на `3`» — истинно. «`13` делится на `3`» — ложно.

**Способ 2 — поставить квантор.** Квантор — это слово, которое говорит, про сколько значений переменной идёт речь.

| Квантор | Слова | Что получается |
|---|---|---|
| квантор всеобщности | «для любого», «для каждого», «все» | «Каждое натуральное число `n` делится на `3`» — ложно |
| квантор существования | «существует», «найдётся», «некоторые» | «Существует натуральное `n`, которое делится на `3`» — истинно |

Дальше слова «натуральное число» всюду значат `1`, `2`, `3`, … — ноль натуральным числом не считается. Когда ноль нужен, он называется отдельно.

**Обязательно говори, откуда берётся переменная.** Без этого высказывание неполное, и вердикт может перевернуться. «Для любого числа `x` верно, что `x` умножить на `x` не меньше `x`» — для натуральных `x` истинно, а если разрешить дроби, то ложно: при `x` равном одной второй произведение равно одной четвёртой, и это меньше.

### Разбор

Дано предложение с переменной «`x` — делитель числа `12`», где `x` — натуральное число от `1` до `6`. Поставь оба квантора и определи истинность каждого получившегося высказывания.

**Заметь главное.** Из одного предложения с переменной рождаются два разных высказывания, и их вердикты друг от друга не зависят: бывает, что всеобщее ложно, а существование истинно, — как раз этот случай.

**Шаг 1 — план.** Нужно получить два высказывания и два вердикта. Для всеобщности вердикт даёт полная проверка всех шести значений (или один контрпример), для существования — один подходящий пример.

> ✅ **План готов:** Ответ = высказывание с квантором всеобщности и его вердикт + высказывание с квантором существования и его вердикт.

**Шаг 2 — квантор всеобщности.** Нужно накрыть словом «каждое» все значения от `1` до `6`: «Каждое натуральное число от `1` до `6` является делителем числа `12`». Проверяю подряд: `1` — да, `2` — да, `3` — да, `4` — да, `5` — нет.
→ **ложно, контрпример `x = 5`**

**Шаг 3 — квантор существования.** Нужен один подходящий `x`: «Среди натуральных чисел от `1` до `6` найдётся делитель числа `12`». Беру `x = 1`: единица делит всё.
→ **истинно, пример `x = 1`**

**Шаг 4 — сборка.** Высказывание с «каждое» ложно (контрпример `5`), высказывание с «найдётся» истинно (пример `1`). Из одного предложения — два высказывания с разными вердиктами.

**Типичная ошибка:** не указать множество, из которого берётся переменная, и спорить о вердикте впустую. «Для любого числа `x` верно, что `x` больше `0`» — для натуральных чисел истинно, а если разрешить `x = 0`, то уже ложно. Спорить тут не о чем: пока не сказано, откуда берётся `x`, вердикта просто нет.

---

## Глава 5. Отрицание утверждений с кванторами

В главах 2 и 3 мы отрицали «все» и «найдётся», сказанные словами. Теперь то же самое, но когда квантор стоит явно и рядом с ним живёт переменная. Правило состоит из двух ходов, и оба обязательны:

> **Меняем квантор на противоположный и отрицаем то, что стоит внутри.**

| Высказывание | Отрицание |
|---|---|
| Для любого `n` верно: `n` обладает свойством | Найдётся `n`, для которого свойство не выполнено |
| Существует `n`, обладающее свойством | Для любого `n` свойство не выполнено |

Если поменять только квантор или отрицать только внутренность — получится не отрицание. Проверяй парой: у высказывания и его отрицания ровно одно из двух истинно.

### Разбор

Построй отрицание высказывания «Существует натуральное число `n`, для которого `2n + 1` делится на `4`» и определи, какое из двух истинно.

**Заметь главное.** Высказывание о существовании подтверждается одним примером — поэтому пробовать числа надо первым делом, это самый дешёвый путь. Но если пример не нашёлся, перебор работу не заканчивает: сколько чисел ни проверь, про все остальные ты по-прежнему не знаешь ничего, а натуральных чисел бесконечно много. Тогда перебор превращается всего лишь в подсказку, а вердикт приходится добывать общей причиной — одной, годной сразу для всех чисел.

**Шаг 1 — план.** Нужно отрицание и вердикт. Отрицание получаю двумя ходами: квантор существования → квантор всеобщности, «делится» → «не делится». Вердикт: сначала поищу пример, а если его не видно — придётся объяснить, почему его нет вообще.

> ✅ **План готов:** Ответ = отрицание с квантором всеобщности + вердикт: либо найденный пример, либо общая причина, что примера нет.

**Шаг 2 — построить отрицание.** Меняю квантор и отрицаю внутренность.
→ **отрицание: «Для любого натурального `n` число `2n + 1` не делится на `4`»**

**Шаг 3 — попробовать найти пример.** Нужно натуральное `n`, при котором `2n + 1` кратно `4`. Считаю: `n = 1` даёт `3`, `n = 2` даёт `5`, `n = 3` даёт `7`, `n = 4` даёт `9`, `n = 5` даёт `11`. Все нечётные, ни одно на `4` не делится.
→ **примера не видно; перебор ничего не доказал, но показал закономерность**

**Шаг 4 — найти общую причину.** Нужно доказать, что примера нет **вообще**, а не только среди пяти проверенных. Число `2n` чётно при любом `n`, значит `2n + 1` нечётно. А всякое число, делящееся на `4`, чётно. Нечётное число не может быть чётным.
→ **ни при каком `n` число `2n + 1` на `4` не делится**

**Шаг 5 — сборка.** Примера нет ни одного, значит исходное высказывание ложно, а его отрицание истинно.
Ответ: отрицание — «Для любого натурального `n` число `2n + 1` не делится на `4`»; исходное ложно, отрицание истинно.

Обрати внимание на границу между шагами 3 и 4: перебор пяти чисел **подсказал** ответ, но доказал его только шаг 4. Утверждение «ни одно» о бесконечном запасе чисел проверкой не берётся.

**Типичная ошибка:** сделать только один из двух ходов. Отрицание высказывания «Каждое натуральное число делится на `2`» — это не «Каждое натуральное число не делится на `2`». Оба ложны (контрпримеры `3` и `4` соответственно), значит это не отрицание. Правильно: «Найдётся натуральное число, которое не делится на `2`».

---

## Глава 6. Понятие логического следования

Запись «если A, то B» называется **следованием**: из A следует B. Здесь A — **условие**, B — **заключение**.

Следование — это обещание: **всякий раз, когда верно A, верно и B**. Больше оно ничего не обещает.

Из этого выходит важное: **про случаи, когда A неверно, следование молчит.** «Если ты уберёшь комнату, я куплю мороженое» — это обещание не запрещает купить мороженое просто так. Нарушено оно будет только в одном случае: комнату убрали, а мороженого нет.

| Комната убрана (A) | Мороженое куплено (B) | Обещание |
|---|---|---|
| да | да | выполнено |
| да | нет | **нарушено** |
| нет | да | не нарушено — про этот случай речи не было |
| нет | нет | не нарушено — про этот случай речи не было |

Эту таблицу можно читать про любое следование, не только про мороженое. Следование «если A, то B» называют **верным**, если строка «нарушено» не случается ни с одним объектом: нигде во всём мире нет предмета, у которого A есть, а B нет. И называют **неверным**, если такой предмет нашёлся хотя бы один. Дальше слова «обещание нарушено» и «следование неверно» значат ровно одно и то же.

Математические примеры следований: «Если число делится на `6`, то оно делится на `3`» (верно). «Если фигура — квадрат, то она прямоугольник» (верно). «Если число чётное, то оно делится на `4`» (неверно: `6`).

### Разбор

Верно ли следование «Если число делится на `12`, то оно делится на `6`»?

**Шаг 1 — план.** Нужен вердикт «верно / неверно». Следование верно, если у **всякого** числа, делящегося на `12`, есть делимость на `6`. Чисел, делящихся на `12`, бесконечно много, значит перебором не обойтись — нужна общая причина, годная сразу для всех. А если такой причины не найдётся, вердикт будет «неверно», и подтвердить его придётся контрпримером — числом, которое делится на `12`, но не делится на `6`.

> ✅ **План готов:** Ответ = «верно», если найдётся причина, годная сразу для всех чисел, делящихся на `12`
>
> Осталось найти: такую причину.

**Шаг 2 — записать условие A через множители.** Нужно понять, что вообще значит «делится на `12`». Это значит, что число можно записать как `12` умножить на какое-то натуральное число: `12`, `24`, `36`, `48`, …
→ **число имеет вид «`12` умножить на что-то»**

**Шаг 3 — вытащить из этого делимость на `6`.** Нужно увидеть в числе множитель `6`. Раскладываю сам множитель: `12 = 6 × 2`. Значит число равно «`6` умножить на `2` умножить на что-то» — множитель `6` в нём есть при любом выборе.
→ **всякое число, делящееся на `12`, делится и на `6`**

**Шаг 4 — сборка.** Причина найдена и работает для всех чисел с условием A сразу, контрпримеру взяться неоткуда.
Ответ: следование верно.

**Типичная ошибка:** считать, что следование запрещает случай «A неверно, а B верно». Из «Если число делится на `12`, то оно делится на `6`» никак не следует, что число `18` не делится на `6`. Число `18` на `12` не делится, значит наше следование про него ничего не обещало — и `18` спокойно делится на `6`.

---

## Глава 7. Отрицание следования

Обещание «если A, то B» ломается **ровно в одном случае из четырёх**: когда условие A выполнено, а заключение B — нет. Во всех остальных случаях обещание держится. Отсюда правило:

> Отрицание «если A, то B» — это «**A выполнено, а B не выполнено**».

Обрати внимание: отрицание следования — само уже **не** следование. Это утверждение о существовании: «найдётся объект, у которого A есть, а B нет». Такой объект — контрпример.

**Заметь главное.** Фраза «если A, то не B» отрицанием **не** является, хотя очень похожа. Проверь на числах. Пусть A — «число делится на `3`», B — «число делится на `9`».

- «Если число делится на `3`, то оно делится на `9`» — ложно, контрпример `6`.
- «Если число делится на `3`, то оно **не** делится на `9`» — тоже ложно, контрпример `9`.

Оба ложны одновременно. Для пары «высказывание и его отрицание» такое невозможно — ровно одно из двух обязано быть истинным. Значит это не она.

### Разбор

Построй отрицание следования «Если число делится на `2` и на `6`, то оно делится на `12`» и определи, какое из двух истинно.

**Шаг 1 — план.** Нужно отрицание и вердикт. Отрицание строю по форме «A выполнено, B нет» — получится утверждение о существовании контрпримера. Значит вердикт сводится к поиску: нашёл такое число → отрицание истинно, следование ложно.

> ✅ **План готов:** Ответ = отрицание в форме «найдётся число, у которого A есть, а B нет» + вердикт по итогу поиска.

**Шаг 2 — построить отрицание.** Здесь A — «число делится на `2` и на `6`», B — «число делится на `12`».
→ **отрицание: «Найдётся число, которое делится на `2` и на `6`, но не делится на `12`»**

**Шаг 3 — искать контрпример.** Нужно число с условием A и без заключения B. Беру самые маленькие кратные `6` (они автоматически кратны и `2`): `6` — на `12` не делится.
→ **контрпример: `6`**

**Шаг 4 — сборка.** Контрпример есть, значит отрицание истинно, а следование ложно.
Ответ: отрицание — «Найдётся число, которое делится на `2` и на `6`, но не делится на `12`»; следование ложно (контрпример `6`), отрицание истинно.

**Типичная ошибка:** записывать отрицание следования как ещё одно следование — «если A, то не B». Настоящее отрицание всегда звучит как «нашёлся один такой объект», и в нём нет слов «если … то».

---

## Глава 8. Обратное утверждение

**Обратное утверждение** к следованию «если A, то B» — это следование «если B, то A»: условие и заключение поменялись местами.

Главное правило темы:

> **Из истинности прямого утверждения истинность обратного не следует.** Это два разных высказывания, и каждое проверяется отдельно.

| Прямое | Верно? | Обратное | Верно? |
|---|---|---|---|
| если число делится на `6`, то делится на `3` | да | если число делится на `3`, то делится на `6` | нет, контрпример `9` |
| если фигура — квадрат, то это прямоугольник | да | если фигура — прямоугольник, то это квадрат | нет, контрпример: прямоугольник `2` на `5` |
| если число оканчивается на `0`, то делится на `10` | да | если число делится на `10`, то оканчивается на `0` | да |

Возможны все четыре сочетания, и вот они все. «Верно и верно» — третья строка таблицы. «Верно, а обратное неверно» — первые две строки. «Неверно, а обратное верно»: прямое «если число делится на `3`, то оно делится на `12`» ложно (контрпример `3`), а обратное «если число делится на `12`, то оно делится на `3`» истинно. «Неверно, и обратное неверно»: прямое «если число делится на `3`, то оно делится на `5`» ложно (контрпример `3`), и обратное «если число делится на `5`, то оно делится на `3`» тоже ложно (контрпример `5`).

**Не путай обратное с отрицанием.** Отрицание следования «если A, то B» — это «нашёлся объект с A и без B». Обратное — это «если B, то A». Совсем разные вещи.

### Разбор

Дано следование «Если число оканчивается на `5`, то оно делится на `5`». Сформулируй обратное утверждение и определи истинность каждого из двух.

**Шаг 1 — план.** Нужно два вердикта — прямому и обратному. Они независимы, значит это две отдельные проверки; в каждой либо нахожу общую причину, либо контрпример.

> ✅ **План готов:** Ответ = вердикт прямому + формулировка обратного + отдельный вердикт обратному.

**Шаг 2 — проверить прямое.** Нужно убедиться, что всякое число с последней цифрой `5` кратно `5`. Разбиваю число на две части: целые десятки и последнюю цифру. Например, `45 = 40 + 5`, `135 = 130 + 5`. Первая часть — это целые десятки, а `10 = 5 × 2`, поэтому любое число десятков делится на `5`. Вторая часть здесь равна `5` и тоже делится на `5`. Складываю два числа, каждое из которых кратно `5`, — сумма тоже кратна `5`.
→ **прямое верно**

**Шаг 3 — сформулировать обратное.** Меняю местами условие и заключение.
→ **обратное: «Если число делится на `5`, то оно оканчивается на `5`»**

**Шаг 4 — проверить обратное.** Нужен контрпример: число, кратное `5`, но с другой последней цифрой. Беру `10`: делится на `5`, а оканчивается на `0`.
→ **обратное неверно, контрпример `10`**

**Шаг 5 — сборка.** Прямое утверждение истинно, обратное ложно.
Ответ: обратное утверждение — «Если число делится на `5`, то оно оканчивается на `5`»; прямое истинно, обратное ложно (контрпример `10`).

**Типичная ошибка:** пользоваться верным правилом задом наперёд. Правило «Если число делится на `4`, то оно чётное» — верное. Ученик смотрит на число `6`, видит, что оно чётное, и заключает: «раз чётное, значит делится на `4`». Тут надо остановиться. Правило начинается со слов «если число делится на `4`» — а про `6` это как раз неизвестно, значит правило про `6` вообще ничего не обещало. И действительно, `6` на `4` не делится.

Иногда правило задом наперёд всё-таки работает — но по другой причине. Признак делимости на `9` верен в обе стороны: и «делится на `9` → сумма цифр делится на `9`», и «сумма цифр делится на `9` → число делится на `9`». Только вторая половина — это отдельный факт, который проверен сам по себе, а не следствие первой. Из того, что прямое правило верно, обратное никогда не выводится.

---

## Глава 9. Следование и равносильность

Иногда верны сразу оба следования: и «если A, то B», и «если B, то A». Тогда говорят, что утверждения A и B **равносильны**, и пишут «A тогда и только тогда, когда B».

Равносильность значит, что A и B — это одно и то же свойство, названное разными словами. Проверяя одно, ты проверяешь и другое.

| A | B | Равносильны? |
|---|---|---|
| число делится на `10` | число оканчивается на `0` | да, оба следования верны |
| число чётное | последняя цифра числа чётная | да, оба следования верны |
| число делится на `6` | число делится на `3` | нет: обратное ложно, контрпример `9` |

**Заметь главное.** Равносильность — это **две** проверки, а не одна. Проверив следование в одну сторону, ты не узнал про вторую сторону ровным счётом ничего: она может быть верной, а может быть ложной.

### Разбор

Равносильны ли утверждения «число делится на `15`» и «число делится на `3` и на `5`»?

**Шаг 1 — план.** Нужен вердикт «равносильны / нет». Равносильность — это два следования сразу, значит проверю их по очереди. Если хоть одно ложно, равносильности нет — и тогда в ответе надо назвать, какая именно сторона сломалась.

> ✅ **План готов:** Ответ = «равносильны», если оба следования верны
>
> Осталось найти: вердикт для стороны «делится на `15`» → «делится на `3` и на `5`» и вердикт для обратной стороны.

**Шаг 2 — проверить сторону «делится на `15`» → «делится на `3` и на `5`».** Нужно вытащить из делимости на `15` две другие делимости. Раскладываю: `15 = 3 × 5`. Число, делящееся на `15`, имеет вид «`15` умножить на что-то», то есть «`3` умножить на `5` умножить на что-то» — в нём есть и множитель `3`, и множитель `5`.
→ **эта сторона верна**

**Шаг 3 — проверить обратную сторону «делится на `3` и на `5`» → «делится на `15`».** Нужно из двух делимостей собрать одну. Число делится на `5`, значит его можно записать как «`5` умножить на что-то»: пусть это `5 × k`. Про то же число известно, что оно делится на `3`. Откуда в нём взяться тройке? Из пятёрки — не может: `3` и `5` взаимно просты, общих делителей, кроме `1`, у них нет. Значит тройку обязан дать второй множитель: `k` делится на `3`, то есть `k` — это `3` умножить на что-то. Тогда всё число равно `5 × 3` умножить на что-то, то есть `15` умножить на что-то. Проверь на числе `60`: оно делится и на `3`, и на `5`; `60 = 5 × 12`, и множитель `12` действительно делится на `3`.
→ **эта сторона тоже верна**

**Шаг 4 — сборка.** Оба следования верны.
Ответ: утверждения равносильны — «число делится на `15`» тогда и только тогда, когда оно делится на `3` и на `5`.

Взаимная простота в шаге 3 — не украшение, а суть дела. Замени `3` и `5` на `2` и `6`: следование «делится на `2` и на `6`» → «делится на `12`» уже ложно, контрпример `6`. Тут `2` и `6` не взаимно просты, двойка у них общая.

**Типичная ошибка:** проверить одну сторону и объявить равносильность. «Каждое число, делящееся на `8`, делится на `4`» — верно. Но `4` делится на `4` и не делится на `8`, значит вторая сторона ложна и утверждения не равносильны.

---

## Глава 10. Следование и свойства предметов

Когда следование связывает два свойства, у его концов есть названия. Пусть верно «если A, то B». Тогда:

- **A — достаточное условие для B.** Хватает знать A, чтобы утверждать B.
- **B — необходимое условие для A.** Без B никакого A не бывает.

Житейский пример: «Если человек — вратарь, то он футболист». Быть вратарём **достаточно**, чтобы быть футболистом. Быть футболистом **необходимо**, чтобы быть вратарём: не футболист вратарём точно не окажется.

Математический пример: «Если число делится на `6`, то оно чётное». Делимость на `6` — достаточное условие чётности. Чётность — необходимое условие делимости на `6`.

Зачем это различие на практике:

| Что известно | Что можно сделать |
|---|---|
| достаточное условие выполнено | сразу объявить, что свойство есть |
| необходимое условие нарушено | сразу отбросить: свойства точно нет |
| необходимое условие выполнено | ничего; проверку это не заканчивает |

Число `35` нечётно — необходимое условие делимости на `6` нарушено, делить не нужно. А число `14` чётно — и это не значит ничего: `14` на `6` не делится.

Если условие сразу и необходимое, и достаточное, — это и есть равносильность.

### Разбор

Для каждого условия определи, достаточное оно для делимости числа на `12`, необходимое, и то и другое или ни то ни другое:
(а) число делится на `24`; (б) число чётное; (в) число делится на `3` и на `4`.

**Заметь главное.** «Достаточное» и «необходимое» — это просто два направления одного следования. Поэтому каждый вердикт получается из двух отдельных проверок: «условие → делится на `12`?» даёт достаточность, «делится на `12` → условие?» даёт необходимость. Первую проверку буду дальше называть коротко **вперёд**, вторую — **назад**. Вперёд отвечает про достаточность, назад — про необходимость.

**Шаг 1 — план.** Нужны три вердикта. Каждый собирается из ответов на два вопроса; сочетание ответов и даёт название.

> ✅ **План готов:** Ответ = для каждого условия пара ответов «достаточно? / необходимо?», переведённая в слово.

**Шаг 2 — условие (а): делится на `24`.** Нужно проверить обе стороны. Вперёд: `24 = 12 × 2`, значит всякое кратное `24` кратно и `12` — достаточно. Назад: число `12` делится на `12`, но не делится на `24` — не необходимо.
→ **(а) достаточное, но не необходимое**

**Шаг 3 — условие (б): число чётное.** Нужно узнать, что чётность даёт и чего не даёт. Проверяю обе стороны. Вперёд: `2` чётно, а на `12` не делится — не достаточно. Назад: всякое кратное `12` содержит множитель `2`, значит чётно — необходимо.
→ **(б) необходимое, но не достаточное**

**Шаг 4 — условие (в): делится на `3` и на `4`.** Нужно проверить обе стороны для пары делимостей сразу. Вперёд: `3` и `4` взаимно просты, поэтому число с обоими множителями делится на `12` — достаточно. Назад: всякое кратное `12` кратно и `3`, и `4`, ведь `12 = 3 × 4` — необходимо.
→ **(в) и достаточное, и необходимое; это условие равносильно делимости на `12`**

**Шаг 5 — сборка.** (а) достаточное, не необходимое. (б) необходимое, не достаточное. (в) и то и другое, то есть равносильное.

**Типичная ошибка:** принять необходимое условие за достаточное. «Чётность необходима для делимости на `12`» — верно. Отсюда делают вывод: «число `10` чётное, значит делится на `12`» — а `10` на `12` не делится. Необходимое условие годится только на то, чтобы **отбрасывать**, а не чтобы подтверждать.
